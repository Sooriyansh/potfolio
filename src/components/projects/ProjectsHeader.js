'use client';
import { motion } from 'framer-motion';
export default function ProjectsHeader({ reducedMotion }) { const move = event => { if (reducedMotion || event.pointerType === 'touch') return; const box = event.currentTarget.getBoundingClientRect(); event.currentTarget.style.setProperty('--spot-x', `${event.clientX - box.left}px`); event.currentTarget.style.setProperty('--spot-y', `${event.clientY - box.top}px`); }; const reveal = reducedMotion ? {} : { initial: { opacity: 0, y: 28 }, animate: { opacity: 1, y: 0 } }; return <header className="projects-intro" onPointerMove={move}><div className="projects-intro-spot" aria-hidden="true"/><motion.p {...reveal} transition={{ duration: .6 }} className="kicker">SELECTED WORK / 2023—NOW</motion.p><motion.h1 {...reveal} transition={{ duration: .75, delay: .08 }}>Products with a reason to exist.</motion.h1><motion.p {...reveal} transition={{ duration: .65, delay: .16 }} className="projects-deck">Each project starts with a real constraint and ends with a clearer, more useful experience.</motion.p><div className="intro-scroll" aria-hidden="true"><i/><span>Scroll to explore</span></div></header>; }
