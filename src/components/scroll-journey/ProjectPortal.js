'use client';
import { useRef } from 'react'; import { useFrame } from '@react-three/fiber'; import * as THREE from 'three';
export default function ProjectPortal({ position, index, progress }) { const ring = useRef(); useFrame((_, delta) => { const active = Math.max(0, 1 - Math.abs(progress.current - (.66 + index * .07)) * 10); ring.current.rotation.z += delta * (.08 + active * .25); ring.current.material.emissiveIntensity = THREE.MathUtils.lerp(ring.current.material.emissiveIntensity, .35 + active * 2.2, .08); }); return <group position={position}><mesh ref={ring}><torusGeometry args={[1.15, .13, 12, 48]}/><meshStandardMaterial color="#25352b" emissive="#84ffbd" emissiveIntensity={.35} roughness={.65}/></mesh><mesh position={[0, 0, -.08]}><circleGeometry args={[1, 32]}/><meshBasicMaterial color={index === 1 ? '#173048' : '#11271e'} transparent opacity={.62}/></mesh></group>; }
