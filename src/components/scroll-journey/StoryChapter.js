'use client';
import Link from 'next/link'; import { ArrowRight, Download } from 'lucide-react'; import { journeyProjects } from '@/data/scrollJourney';
export default function StoryChapter({ chapter, index }) { return <article className="story-chapter" id={`journey-${chapter.id}`}><div className="story-copy"><p className="kicker">{chapter.label}</p><h2>{chapter.headline}</h2><p>{chapter.copy}</p>{index === 3 && <div className="portal-list">{journeyProjects.map((project, i) => <Link href={`/projects/${project.slug}`} key={project.slug}><small>Portal 0{i + 1}</small><strong>{project.title}</strong><span>{project.role} · {project.technology}</span><em>{project.outcome}</em><b>Open case study <ArrowRight/></b></Link>)}</div>}{index === 4 && <div className="journey-actions"><Link className="button light" href="/projects">View projects <ArrowRight/></Link><Link className="button ghost" href="/contact">Contact me</Link><Link className="button ghost" href="/resume">Download resume <Download/></Link></div>}</div></article>; }
