'use client'; import { useState } from 'react'; import { ArrowUpRight, LoaderCircle, Check } from 'lucide-react';
export default function ContactForm(){const [state,setState]=useState('idle'),[error,setError]=useState('');async function submit(e){e.preventDefault();setState('loading');setError('');const data=Object.fromEntries(new FormData(e.currentTarget));try{const r=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const j=await r.json();if(!r.ok)throw new Error(j.error);setState('success');e.target.reset()}catch(x){setError(x.message);setState('error')}}return <form className="contact-form" onSubmit={submit}><div className="form-grid"><label>Name *<input name="name" required maxLength="80" placeholder="Your name"/></label><label>Email *<input name="email" type="email" required placeholder="you@company.com"/></label><label>Company<input name="company" placeholder="Company or team"/></label><label>Project type *<select name="projectType" required defaultValue=""><option value="" disabled>Select one</option><option>Web application</option><option>Mobile application</option><option>Backend system</option><option>Collaboration</option></select></label><label className="wide">Budget range *<select name="budget" required defaultValue=""><option value="" disabled>Select a range</option><option>Under ₹50k</option><option>₹50k – ₹2L</option><option>₹2L – ₹5L</option><option>Let’s discuss</option></select></label><label className="wide">What are we solving? *<textarea name="message" required minLength="20" maxLength="2000" rows="5" placeholder="A little context, the challenge, and what success looks like…"/></label><input className="trap" name="website" tabIndex="-1" autoComplete="off"/></div><button className="button light" disabled={state==='loading'}>{state==='loading'?<LoaderCircle className="spin"/>:state==='success'?<Check/>:<ArrowUpRight/>}{state==='success'?'Message sent':'Start a conversation'}</button>{error&&<p className="form-error" role="alert">{error}</p>}</form>}
