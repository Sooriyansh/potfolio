'use client'; import {useState} from 'react'; import Image from 'next/image'; import Link from 'next/link'; import {ArrowRight} from 'lucide-react';
export default function ProjectGrid({projects}){const [filter,setFilter]=useState('All');const cats=['All','Web Applications','Mobile Applications','Backend Systems','Experiments'];const shown=filter==='All'?projects:projects.filter(p=>p.category===filter);return <><div className="filters">{cats.map(x=><button className={filter===x?'active':''} onClick={()=>setFilter(x)} key={x}>{x}</button>)}</div><div className="projects-grid">{shown.map(p=><article className="project-card" key={p.slug}><Link href={`/projects/${p.slug}`}><div className="project-visual"><Image src={p.image} alt={`${p.title} case study preview`} fill sizes="(max-width:900px) 100vw, 50vw"/></div><p className="kicker">{p.category}</p><h2>{p.title}</h2><p>{p.summary}</p><span className="text-link">View case study <ArrowRight/></span></Link></article>)}</div></>}
