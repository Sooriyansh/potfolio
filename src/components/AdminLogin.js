'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';export default function AdminLogin(){const [error,setError]=useState(''),router=useRouter();async function go(e){e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});if(r.ok)router.push('/admin/dashboard');else setError('Invalid email or password.')}return <form className="admin-card" onSubmit={go}><p className="kicker">SECURE ADMIN</p><h1>Sign in</h1><label>Email<input name="email" type="email" required/></label><label>Password<input name="password" type="password" required/></label><button className="button light">Continue</button>{error&&<p className="form-error">{error}</p>}</form>}
