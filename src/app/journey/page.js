import {journey} from '@/data/portfolio';export const metadata={title:'Journey',description:'A practical timeline of learning through building.'};export default function Page(){return <><header className="inner-hero"><p className="kicker">THE JOURNEY</p><h1>Progress is rarely a straight line.</h1><p>Skills arrived through projects, broken builds, unfamiliar constraints, and the habit of returning with a better question.</p></header><section className="section">{journey.map(([n,t,p])=><article className="journey-row" key={n}><b>{n}</b><h3>{t}</h3><p>{p}</p></article>)}</section></>}
